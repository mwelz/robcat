#include <Rcpp.h>
using namespace Rcpp;

NumericMatrix outer_vec(NumericVector);

NumericMatrix matplus(NumericMatrix, NumericMatrix);

NumericMatrix mmult(NumericMatrix, NumericMatrix);

NumericMatrix mat2vec(NumericVector);
